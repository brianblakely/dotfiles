## Do not expose microphone-class ALSA cards as playback devices.

monitor.alsa.rules = [
  {
    matches = [
      {
        alsa.id = "Microphone"
        api.alsa.pcm.stream = "playback"
      }
    ]
    actions = {
      update-props = {
        node.disabled = true
      }
    }
  }
]
